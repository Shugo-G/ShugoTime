services:
  web:
    build: .
    volumes:
      - ./data:/app/data
    environment:
      - SQLITE_PATH=/app/data/db.sqlite3
      - FICHADAS_DIR=/app/data/fichadas
    env_file:
      - .env
    restart: unless-stopped
